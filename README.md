# Adaptive Image Enhancement

Веб-приложение для автоматической коррекции изображений в браузере пользователя.  
Система анализирует изображение локально, подбирает коэффициенты улучшения и применяет коррекцию без отправки файла на сервер.

## Демо

Проект доступен по [ссылке](https://baga21bruh.github.io/Project_VK_images/)

## Описание

Приложение улучшает изображения по трём параметрам:
- яркость;
- контрастность;
- цветность.

Обработка выполняется полностью на стороне клиента. Пользователь загружает изображение, запускает задачу, отслеживает прогресс и получает готовый результат для скачивания.

## Возможности

- загрузка изображений через веб-интерфейс;
- создание задачи на обработку;
- отображение статуса и прогресса;
- отмена активной задачи;
- предпросмотр исходного и обработанного изображения;
- скачивание итогового файла;
- фоновая обработка без блокировки интерфейса;
- поддержка форматов JPG, JPEG, PNG, BMP, HEIC и HEIF.

## Принцип работы

1. Пользователь выбирает файл в интерфейсе.
2. Приложение создаёт задачу и присваивает ей идентификатор.
3. Исходное изображение передаётся в Web Worker.
4. На этапе декодирования:
   - стандартные форматы открываются средствами браузера;
   - HEIC и HEIF при необходимости предварительно конвертируются.
5. Изображение уменьшается до фиксированного размера для подачи в модель.
6. ML-модель предсказывает коэффициенты коррекции:
   - яркости;
   - контрастности;
   - цветности.
7. Коэффициенты применяются к исходному изображению в полном разрешении.
8. Результат кодируется и возвращается в интерфейс.

## Подход к ML-части

В проекте используется регрессия глобальных параметров изображения.  
Модель предсказывает не само улучшенное изображение, а набор коэффициентов, которые затем применяются отдельным алгоритмом постобработки.

Такой подход выбран из-за:
- компактного размера модели;
- низкой вычислительной нагрузки;
- быстрого запуска в браузере;
- удобной интеграции с клиентским приложением.

## Подготовка модели

Для обучения использовались эталонные изображения и их синтетически ухудшенные версии.  
На этапе подготовки данных варьировались параметры яркости, контрастности и цветности, после чего рассчитывались коэффициенты восстановления.

В результате модель обучалась предсказывать вектор из трёх значений:
- коэффициент коррекции яркости;
- коэффициент коррекции контрастности;
- коэффициент коррекции цветности.

Обучение выполнялось во внешней среде на Python с использованием TensorFlow и Keras, после чего модель была экспортирована для браузерного инференса через TensorFlow.js.

## Технологии

### ML
- Python
- TensorFlow
- Keras
- TensorFlow.js

### Frontend
- React
- Vite
- JavaScript
- CSS

### Обработка в браузере
- Web Worker
- Canvas / OffscreenCanvas
- createImageBitmap

### Поддержка форматов
- стандартное декодирование браузера для JPG, PNG и BMP;
- heic-to для HEIC и HEIF.

## Архитектура

Проект разделён на несколько логических слоёв.

### Интерфейс
Отвечает за:
- загрузку изображений;
- отображение задач;
- отображение прогресса;
- предпросмотр исходного и обработанного изображения;
- скачивание результата.

### Управление задачами
Отвечает за:
- создание задачи;
- хранение текущего состояния;
- обновление прогресса;
- отмену обработки;
- передачу событий в UI.

### Worker-пайплайн
Отвечает за:
- декодирование изображения;
- подготовку входа для модели;
- запуск инференса;
- применение коэффициентов коррекции;
- экспорт результата.

### ML-слой
Отвечает за:
- загрузку модели;
- подготовку тензора;
- запуск предсказания;
- нормализацию итоговых коэффициентов.

## Структура проекта

```text
project/
├── public/
│   ├── models/
│   │   └── photo_regressor/
│   │       ├── group1-shard1of1.bin
│   │       ├── inference_config.json
│   │       └── model.json
│   ├── screenshots/
│   ├── favicon.svg
│   └── icons.svg
├── src/
│   ├── app/
│   │   ├── App.jsx
│   │   └── main.jsx
│   ├── assets/
│   ├── features/
│   │   └── enhancer/
│   │       ├── api/
│   │       ├── model/
│   │       ├── pipeline/
│   │       ├── ui/
│   │       └── worker/
│   ├── shared/
│   │   ├── constants/
│   │   └── utils/
│   └── styles/
├── training model/
│   └── ML_photo_upgrader.ipynb
├── index.html
├── package.json
├── vite.config.js
└── README.md
```

## Установка и запуск

### Требования
- Node.js
- npm

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

### Сборка production-версии
```bash
npm run build
```

### Локальный просмотр production-сборки
```bash
npm run preview
```

## Ограничения

Текущая версия ориентирована на запуск в современных браузерах с поддержкой необходимых Web API.  
Скорость обработки зависит от разрешения изображения, ресурсов устройства и поддержки аппаратного ускорения.

## Дальнейшее развитие

Возможные направления развития проекта:
- добавление дополнительных режимов коррекции;
- расширение набора оцениваемых параметров;
- улучшение визуализации этапов обработки;
- сравнение нескольких результатов обработки;
- адаптация модели под более широкий набор сценариев съёмки.
